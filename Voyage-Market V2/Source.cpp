#include "Header.hpp"

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	int size = 1;
	std::string* loginArr = new std::string[size];
	std::string* roleArr = new std::string[size];
	std::string* passwordArr = new std::string[size];


	return 0;
}